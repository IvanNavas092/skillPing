import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'skillping.tfg',
  appName: 'ProyectoTfg_A16_v2',
  webDir: 'dist/ProyectoTfg_A16_v2'
};

export default config;
